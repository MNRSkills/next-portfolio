import React from 'react'

const BlogDetail = (props) => {
    console.log(props)
    // const { title, } = props.blogPost.fields;
  return (
    <div>BlogDetail</div>
  )
}

export default BlogDetail;