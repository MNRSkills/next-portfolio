
const ProfileItem = (props) => {

    const { firstName, lastName, skill, fourLineBio} = props.profile;
    return (
        <div>

            <div>
                Hi
            </div>
            <div>
            </div>
        </div>
    )
}

export default ProfileItem;